spring:
  application:
    name: 9roomUniv
  datasource:
    url: jdbc:mysql://localhost:3306/db_9roomUniv?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&useSSL=false
    username: root # ??? ??? ???
    password: 7897 # ??? ??? ????
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create # ?? ??? update, ?? ??? validate? none?? ??
      show-sql: true # SQL ?? ?? ????? true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
  api-docs:
    path: /v1/api-docs
    enabled: true
  default-produces-media-type: application/json
  default-consumes-media-type: application/json

  portone:
    webhook:
      secret: "{WEBHOOK_V2_SECRET}"